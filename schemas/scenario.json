{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://expeca.proj.kth.se/scenario.json",
    "title": "WorkloadScenario",
    "description": "A workload scenario for the testbed, running one or more workloads in parallel.",
    "type": "object",
    "additionalProperties": {"$ref": "workload.json" },
    "required": [],
    "properties": {},
    "examples": [
        {
            "workload_wifi": {
                "client-device": "generic",
                "num-clients": 3,
                "client-workload": "client-test-wkld-1",
                "backend-workload": "backend-test-wkld-1",
                "network": {
                    "base": "wifiap",
                    "client": "native"
                }
            }
        }
    ]
}